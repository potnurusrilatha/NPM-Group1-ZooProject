 <div class="animal_names">
            <% animal_names.forEach(animal => { %>
                <a href="/<%= animal.groupName %>/<%= animal.name.replace(" ", "-")%>"><%= animal.name.replace(" ", "-") %></a><br>
            <% }) %>
        </div>
   

